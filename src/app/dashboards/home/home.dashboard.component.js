import template from './templates/home.dashboard.template.html';
import controller  from './home.dashboard.controller';

const homeDashboard = {
  controller,
  template
};

export default homeDashboard;
