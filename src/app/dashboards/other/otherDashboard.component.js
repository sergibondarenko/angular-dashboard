import template from './templates/otherDashboard.template.html';
import controller from './otherDashboard.controller';

const otherDashboard = {
  controller,
  template
};

export { otherDashboard };
