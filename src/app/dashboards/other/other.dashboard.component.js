import template from './templates/other.dashboard.template.html';
import controller from './other.dashboard.controller';

const otherDashboard = {
  controller,
  template
};

export default otherDashboard;
